/** @type {import('next').NextConfig} */
const nextConfig = {
  // NEXT_PUBLIC_API_URL is baked in at build time.
  // Set it in your Vercel project env vars for production.
};

export default nextConfig;
