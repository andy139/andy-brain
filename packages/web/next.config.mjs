/** @type {import('next').NextConfig} */
const nextConfig = {
  // NEXT_PUBLIC_API_URL is baked in at build time.
  // Set it in your Railway web service env vars for production.
};

export default nextConfig;
